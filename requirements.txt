# Python 3.11+
#
# Pin exact versions (==) to ensure reproducible installs and
# avoid silent upgrades that may introduce breaking changes or vulnerabilities.
# Run `pip install -r requirements.txt` inside .venv to install.

# Core RAG stack
openai==2.30.0
qdrant-client==1.17.1
tiktoken==0.12.0
llama-index-core==0.14.19  # swap to llama-index if extra integrations are needed

# Document loading
pypdf==6.9.2
python-docx==1.2.0

# Environment
python-dotenv==1.2.2

# UI
streamlit==1.56.0

# Evaluation
# Note: ragas pulls in diskcache 5.6.3 which has CVE-2025-69872 (pickle deserialization).
# No fix version exists yet. Risk is acceptable for local dev — attacker needs local write access.
# Revisit before any shared/production deployment.
ragas==0.4.3

# Testing
pytest==9.0.2
